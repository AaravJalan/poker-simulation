# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
Pillow>=10.0.0

# Build / bindings (optional; CMake FetchContent can provide pybind11)
pybind11>=2.11.0

# Utilities
numpy>=1.24.0
bcrypt>=4.0.0

# Card detection (OpenCV)
opencv-python-headless>=4.8.0

# AI chatbot (optional; set OPENAI_API_KEY for GPT responses)
openai>=1.0.0
